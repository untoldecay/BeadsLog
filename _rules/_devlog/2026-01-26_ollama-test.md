# Ollama Extraction Test

**Date:** 2026-01-26

We decided to switch from Redis to Memcached for session storage.
The `SessionStore` interface was updated.
Also noticed that `auth-service` is consuming too much memory.

 
 
 
